// had to move to typescript, will hopefully be cleaned
// up in future revisions, right now a pain to look through
export class Player{
    searchArg: string;
    constructor(searchArg: string) {
        this.searchArg = searchArg;
    }
}